name: pr-readiness

on:
  pull_request:

jobs:
  backend-pr-readiness:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y libpq-dev pkg-config

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy

      - name: Run PR readiness checks
        shell: pwsh
        env:
          PR_BASE_SHA: ${{ github.event.pull_request.base.sha }}
        run: ./scripts/pr-readiness.ps1 -RepoRoot . -BackendRoot ./backend -Ci

# SECURITY REVIEW (SecureByDesign v1.1.0 - REGLEMENTE)
# Controls reviewed: SBD-01 to SBD-25.
# Verified in this file:
#   - OK SBD-07: no repository secret is hardcoded; CI consumes GitHub-provided metadata only.
#   - OK SBD-11: every pull request executes the readiness checks automatically.
#   - OK SBD-22: enforcement is versioned and reproducible in CI.
# Not fully satisfiable in this file:
#   - WARN SBD-08: CI cannot prove production TLS or at-rest encryption on its own.
#     Alternative: combine this workflow with deployment policy checks and runtime validation.
