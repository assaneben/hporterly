CREATE TABLE IF NOT EXISTS ws_presence (id UUID PRIMARY KEY DEFAULT gen_random_uuid(), username VARCHAR(120) NOT NULL, session_id VARCHAR(120) NOT NULL, connected_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), last_seen_at TIMESTAMPTZ NOT NULL DEFAULT NOW());
